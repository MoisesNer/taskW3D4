//Function 2: convert minutes into seconds. 
//Function will take one parameter that will be a number 
//of minutes, up to 2 decimal places. Convert it into seconds, 
//and return that number.

const minToSecs = function (x) {

    return (x * 60) ;

}
console.log(minToSecs(12.5))
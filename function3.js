//Function 3: convert hours into seconds. 
//Take in a value of hours up to 4 decimal places,
//and convert it into seconds. 

const hoursToSecs = function (x) {

    return (x * 3600) ;

}
console.log(hoursToSecs(15.9999))